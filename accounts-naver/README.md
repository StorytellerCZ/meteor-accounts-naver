# accounts-naver

A login service for [NAVER](https://www.naver.com) accounts.

This package registers the service with Meteor. Get Client ID and Client Secret from NAVER and put them in `settings.json` as described in the `storyteller:naver-config-ui` README. Runtime options (`loginStyle`, callback URL) are in `storyteller:naver-oauth`.
